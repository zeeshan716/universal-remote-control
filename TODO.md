* \-
* Complete Bluetooth setup 
* If a user change the IP and PORT addresses, the user should be able to save them (personalize them)
* if the connection is no more, then if user tries to send something, app should show them warning
* then "Sending..." should show up for some moments, and after it should go back to idle or robot offline 
* Drone setup
* some lottie animations may be

